{
  "name": "vue-advanced-workshop",
  "description": "repo for Vue advanced workshop",
  "private": true,
  "scripts": {
    "test": "jest --env node",
    "watch": "jest --env node --watch"
  },
  "dependencies": {
    "jest": "^21.0.1",
    "jsdom": "^11.2.0",
    "vue": "^2.4.2"
  }
}
